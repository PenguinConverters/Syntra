<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <packageSources>
    <clear />
    <add key="nuget.org" value="https://api.nuget.org/v3/index.json" protocolVersion="3" />

    <!--
      The Keyra SDK (PenguinConverters.Keyra*) is proprietary and separately licensed, but
      it is published on nuget.org, so every solution here restores from this one source
      with no credentials. Using it in your own product still requires a Keyra licence -
      see NOTICE.
    -->
  </packageSources>
</configuration>
